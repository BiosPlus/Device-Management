---
title: "Google Groups"
description: "Dynamic group queries that auto-calculate membership based on parameters — no more manually adding new users."
---

# Dynamic Groups

<Note>
Suspended users are excluded from all queries below. If you want suspended users included for some reason, remove `&& user.suspended == false` from the end of each query.
</Note>

#### APAC Dynamic Group

```sql
user.addresses.exists(address, address.country_code=='AS' || address.country_code=='AU' || address.country_code=='BD' || address.country_code=='BN' || address.country_code=='BT' || address.country_code=='CC' || address.country_code=='CN' || address.country_code=='CX' || address.country_code=='FJ' || address.country_code=='FM' || address.country_code=='GU' || address.country_code=='HK' || address.country_code=='ID' || address.country_code=='IN' || address.country_code=='IO' || address.country_code=='JP' || address.country_code=='KH' || address.country_code=='KI' || address.country_code=='KP' || address.country_code=='KR' || address.country_code=='LA' || address.country_code=='LK' || address.country_code=='MH' || address.country_code=='MM' || address.country_code=='MN' || address.country_code=='MO' || address.country_code=='MP' || address.country_code=='MV' || address.country_code=='MY' || address.country_code=='NF' || address.country_code=='NP' || address.country_code=='NR' || address.country_code=='NU' || address.country_code=='NZ' || address.country_code=='PG' || address.country_code=='PH' || address.country_code=='PK' || address.country_code=='PN' || address.country_code=='PW' || address.country_code=='SB' || address.country_code=='SG' || address.country_code=='TH' || address.country_code=='TK' || address.country_code=='TL' || address.country_code=='TO' || address.country_code=='TV' || address.country_code=='TW' || address.country_code=='UM' || address.country_code=='VN' || address.country_code=='VU' || address.country_code=='WS') && user.suspended == false
```

#### EMEA Dynamic Group

```sql
user.addresses.exists(address, address.country_code=='AD' || address.country_code=='AE' || address.country_code=='AF' || address.country_code=='AL' || address.country_code=='AM' || address.country_code=='AO' || address.country_code=='AQ' || address.country_code=='AT' || address.country_code=='AX' || address.country_code=='AZ' || address.country_code=='BA' || address.country_code=='BE' || address.country_code=='BF' || address.country_code=='BG' || address.country_code=='BH' || address.country_code=='BI' || address.country_code=='BJ' || address.country_code=='BL' || address.country_code=='BV' || address.country_code=='BW' || address.country_code=='BY' || address.country_code=='CD' || address.country_code=='CF' || address.country_code=='CG' || address.country_code=='CH' || address.country_code=='CI' || address.country_code=='CK' || address.country_code=='CM' || address.country_code=='CV' || address.country_code=='CW' || address.country_code=='CY' || address.country_code=='CZ' || address.country_code=='DE' || address.country_code=='DJ' || address.country_code=='DK' || address.country_code=='DZ' || address.country_code=='EE' || address.country_code=='EG' || address.country_code=='EH' || address.country_code=='ER' || address.country_code=='ES' || address.country_code=='ET' || address.country_code=='FI' || address.country_code=='FO' || address.country_code=='FR' || address.country_code=='GA' || address.country_code=='GB' || address.country_code=='GE' || address.country_code=='GF' || address.country_code=='GG' || address.country_code=='GH' || address.country_code=='GI' || address.country_code=='GL' || address.country_code=='GM' || address.country_code=='GN' || address.country_code=='GP' || address.country_code=='GQ' || address.country_code=='GR' || address.country_code=='GW' || address.country_code=='HM' || address.country_code=='HR' || address.country_code=='HU' || address.country_code=='IE' || address.country_code=='IL' || address.country_code=='IM' || address.country_code=='IQ' || address.country_code=='IR' || address.country_code=='IS' || address.country_code=='IT' || address.country_code=='JE' || address.country_code=='JO' || address.country_code=='KE' || address.country_code=='KG' || address.country_code=='KM' || address.country_code=='KW' || address.country_code=='KZ' || address.country_code=='LB' || address.country_code=='LI' || address.country_code=='LR' || address.country_code=='LS' || address.country_code=='LT' || address.country_code=='LU' || address.country_code=='LV' || address.country_code=='LY' || address.country_code=='MA' || address.country_code=='MC' || address.country_code=='MD' || address.country_code=='ME' || address.country_code=='MF' || address.country_code=='MG' || address.country_code=='MK' || address.country_code=='ML' || address.country_code=='MQ' || address.country_code=='MR' || address.country_code=='MT' || address.country_code=='MU' || address.country_code=='MW' || address.country_code=='MZ' || address.country_code=='NA' || address.country_code=='NC' || address.country_code=='NE' || address.country_code=='NG' || address.country_code=='NL' || address.country_code=='NO' || address.country_code=='OM' || address.country_code=='PF' || address.country_code=='PL' || address.country_code=='PM' || address.country_code=='PS' || address.country_code=='PT' || address.country_code=='QA' || address.country_code=='RE' || address.country_code=='RO' || address.country_code=='RS' || address.country_code=='RU' || address.country_code=='RW' || address.country_code=='SA' || address.country_code=='SC' || address.country_code=='SD' || address.country_code=='SE' || address.country_code=='SH' || address.country_code=='SI' || address.country_code=='SK' || address.country_code=='SL' || address.country_code=='SM' || address.country_code=='SN' || address.country_code=='SO' || address.country_code=='SS' || address.country_code=='ST' || address.country_code=='SX' || address.country_code=='SY' || address.country_code=='SZ' || address.country_code=='TD' || address.country_code=='TF' || address.country_code=='TG' || address.country_code=='TJ' || address.country_code=='TM' || address.country_code=='TN' || address.country_code=='TR' || address.country_code=='TZ' || address.country_code=='UA' || address.country_code=='UG' || address.country_code=='UZ' || address.country_code=='VA' || address.country_code=='WF' || address.country_code=='YE' || address.country_code=='YT' || address.country_code=='ZA' || address.country_code=='ZM' || address.country_code=='ZW') && user.suspended == false
```

#### LATAM Dynamic Group

```sql
user.addresses.exists(address, address.country_code=='AG' || address.country_code=='AI' || address.country_code=='AR' || address.country_code=='AW' || address.country_code=='BB' || address.country_code=='BM' || address.country_code=='BO' || address.country_code=='BR' || address.country_code=='BS' || address.country_code=='BZ' || address.country_code=='CL' || address.country_code=='CO' || address.country_code=='CR' || address.country_code=='CU' || address.country_code=='DM' || address.country_code=='DO' || address.country_code=='EC' || address.country_code=='FK' || address.country_code=='GD' || address.country_code=='GS' || address.country_code=='GT' || address.country_code=='GY' || address.country_code=='HN' || address.country_code=='HT' || address.country_code=='JM' || address.country_code=='KN' || address.country_code=='KY' || address.country_code=='LC' || address.country_code=='MS' || address.country_code=='MX' || address.country_code=='NI' || address.country_code=='PA' || address.country_code=='PE' || address.country_code=='PR' || address.country_code=='PY' || address.country_code=='SR' || address.country_code=='SV' || address.country_code=='TC' || address.country_code=='TT' || address.country_code=='UY' || address.country_code=='VC' || address.country_code=='VE' || address.country_code=='VG' || address.country_code=='VI') && user.suspended == false
```

#### NA Dynamic Group

```sql
user.addresses.exists(address, address.country_code=='CA' || address.country_code=='US') && user.suspended == false
```

#### Global Dynamic Group (All Staff)

```sql
(user.is_mailbox_setup==true && user.archived==false) && user.suspended == false
```
